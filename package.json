{
  "name": "tree-sitter-hml",
  "version": "0.1.0",
  "description": "HML is a small markup language that looks a bit like HTML, but uses block syntax with curly braces.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hypermark-lang/tree-sitter-hml.git"
  },
  "funding": "",
  "license": "MIT",
  "author": {
    "name": "Eduardo Flores",
    "email": "edfloreshz@gmail.com",
    "url": "https://edfloreshz.dev"
  },
  "main": "bindings/node",
  "types": "bindings/node",
  "keywords": [
    "incremental",
    "parsing",
    "tree-sitter",
    "hml"
  ],
  "files": [
    "grammar.js",
    "tree-sitter.json",
    "binding.gyp",
    "prebuilds/**",
    "bindings/node/*",
    "queries/*",
    "src/**",
    "*.wasm"
  ],
  "dependencies": {
    "node-addon-api": "^8.5.0",
    "node-gyp-build": "^4.8.4"
  },
  "devDependencies": {
    "prebuildify": "^6.0.1",
    "tree-sitter": "^0.25.0",
    "tree-sitter-cli": "^0.26.6"
  },
  "peerDependencies": {
    "tree-sitter": "^0.25.0"
  },
  "peerDependenciesMeta": {
    "tree-sitter": {
      "optional": true
    }
  },
  "scripts": {
    "install": "node-gyp-build",
    "prestart": "tree-sitter build --wasm",
    "start": "tree-sitter playground",
    "test": "node --test bindings/node/*_test.js"
  }
}
